# def my_agent():
def kuchbhi():    #kuchbhi = main
    a = 10
    b = [10, 20, 30]
    print(a, type(a), id(a))
    print(b, type(b), bin(id(b)))  # -> id(a): decimal, hex(), oct, bin


if __name__ == '__main__':        #functin main bnaya then this step is manatry t write t run
    # my_agent() can be any name of main
    kuchbhi()


# ——————————————————————————

#immutabe=data cant mdify = need for read only purpse
#every functin has its wn frame = stred in frame in every memry representatin 
